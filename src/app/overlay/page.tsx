import { OverlayScreen } from "@/features/livemessage/components/overlay/OverlayScreen";

export const metadata = {
  title: "Overlay",
};

export default function OverlayPage() {
  return <OverlayScreen />;
}

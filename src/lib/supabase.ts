// Re-exportações centralizadas para compatibilidade.
//
// Uso exclusivo server-side (Server Components / Services / API Routes).

export { supabaseServer, getAvatarPublicUrl } from "./supabase-server";
